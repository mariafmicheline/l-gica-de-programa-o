t = float(input('Qual o custo total do espetáculo?'))
c = float(input('Quanto custa cada convite?'))
q = t/c
print('A quantidade de convites que devem ser vendidos,para que o custo do espetaculo seja alcançado, é: {}'.format(q))